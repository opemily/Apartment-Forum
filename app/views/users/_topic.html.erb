<h3> <%= link_to(topic.title, topic_posts_path(topic)) %> </h3>

  <%= render(partial: 'post', collection: @posts.where(topic_id: topic.id))|| '<p> You have not posted anything in this topic</p>'.html_safe  %>

